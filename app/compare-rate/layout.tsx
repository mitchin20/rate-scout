export default function CompareRateLayout({ children }: { children: React.ReactNode }) {
    return (
        <section data-theme="light" className="mx-auto max-w-5xl p-6">
            {children}
        </section>
    );
}
